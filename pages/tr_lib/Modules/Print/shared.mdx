# Shared
import { Callout } from 'nextra/components'
import { Tabs } from 'nextra/components'

a better way to print, so you can later look up and track where your codes at

## lib.print.{...}

<Tabs items={['Debug', 'Error', 'Warn', 'Info', 'Success', 'Log']}>
<Tabs.Tab>
## Debug

```lua
lib.print.debug(message, path, line)
```

### Parameters
* message: `any`
* path: `debug.getinfo(1, "Sl").short_src`
* line: `debug.getinfo(1, "Sl").currentline`

### Example

```lua filename="wrapper/print.lua"
function print(parameter)
    lib.print.debug(parameter)
end
```

```lua filename="shared/main.lua"
local retval = 'Hello Lenix'

print(retval, debug.getinfo(1, "Sl").short_src, debug.getinfo(1, "Sl").currentline)
```

<Callout type="info">
its highly recommended to use debug.getinfo on the debug mode
</Callout>
</Tabs.Tab>
<Tabs.Tab>
## Error

```lua
lib.print.error(message, path, line)
```

### Parameters
* message: `any`
* path: `debug.getinfo(1, "Sl").short_src`
* line: `debug.getinfo(1, "Sl").currentline`

### Example

```lua filename="wrapper/print.lua"
function print(parameter)
    lib.print.error(parameter)
end
```

```lua filename="shared/main.lua"
local retval = 'Hello Lenix'

print(retval, debug.getinfo(1, "Sl").short_src, debug.getinfo(1, "Sl").currentline)
```
<Callout type="info">
please refer to the important note above about path and line parameters regarding ERROR type
</Callout>
</Tabs.Tab>
<Tabs.Tab>
## Warn

```lua
lib.print.warn(message, path, line)
```

### Parameters
* message: `any`
* path: `debug.getinfo(1, "Sl").short_src`
* line: `debug.getinfo(1, "Sl").currentline`

### Example

```lua filename="wrapper/print.lua"
function print(parameter)
    lib.print.warn(parameter)
end
```

```lua filename="shared/main.lua"
local retval = 'Hello Lenix'

print(retval, debug.getinfo(1, "Sl").short_src, debug.getinfo(1, "Sl").currentline)
```
</Tabs.Tab>
<Tabs.Tab>
## Info

```lua
lib.print.info(message, path, line)
```

### Parameters
* message: `any`
* path: `debug.getinfo(1, "Sl").short_src`
* line: `debug.getinfo(1, "Sl").currentline`

### Example

```lua filename="wrapper/print.lua"
function print(parameter)
    lib.print.info(parameter)
end
```

```lua filename="shared/main.lua"
local retval = 'Hello Lenix'

print(retval, debug.getinfo(1, "Sl").short_src, debug.getinfo(1, "Sl").currentline)
```
</Tabs.Tab>
<Tabs.Tab>
## Success

```lua
lib.print.success(message, path, line)
```

### Parameters
* message: `any`
* path: `debug.getinfo(1, "Sl").short_src`
* line: `debug.getinfo(1, "Sl").currentline`

### Example

```lua filename="wrapper/print.lua"
function print(parameter)
    lib.print.success(parameter)
end
```

```lua filename="shared/main.lua"
local retval = 'Hello Lenix'

print(retval, debug.getinfo(1, "Sl").short_src, debug.getinfo(1, "Sl").currentline)
```
</Tabs.Tab>
<Tabs.Tab>
## Log

```lua
lib.print.log(message, path, line)
```

### Parameters
* message: `any`
* path: `debug.getinfo(1, "Sl").short_src`
* line: `debug.getinfo(1, "Sl").currentline`

### Example

```lua filename="wrapper/print.lua"
function print(parameter)
    lib.print.log(parameter)
end
```

```lua filename="shared/main.lua"
local retval = 'Hello Lenix'

print(retval, debug.getinfo(1, "Sl").short_src, debug.getinfo(1, "Sl").currentline)

```
</Tabs.Tab>
</Tabs>

### Conditions

* valid print type
* defined message field
* only the following aliases are allowed:
  * error = err
  * warning = warn
  * info = inf

### Notice

<Callout type="warning">
path and line are static values "as is" (except you know what you are doing) and also not required to be passes, they help for debugging and track the location of the actual print, except the ERROR type, when you ignore them, you will be switching automatically to a returnal error that stops the execution of the rest of the code
</Callout>